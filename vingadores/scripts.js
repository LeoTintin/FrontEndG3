function showDescription(colunalink) {
    var descricao = colunaLink.querySelector('.desc');
    descricao.style.opacity = '1';
}

function hideDescription(colunalink) {
    var descricao = colunaLink.querySelector('.desc');
    descricao.style.opacity = '0';
}